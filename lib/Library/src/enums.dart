enum Region {
  na,
  eu,
  ap,
  ko,
}

enum HttpMethod {
  post,
  get,
  put,
  delete,
}

enum CurrencyType {
  valorantPoints,
  radianitePoints,
  freeAgent,
}

enum ItemType {
  buddy,
  playercard,
  spray,
  title,
  weapon,
  unknown,
}
